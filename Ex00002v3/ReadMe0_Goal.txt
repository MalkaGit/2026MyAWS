Goal:
    run locally, CRUD with rds on aws

    incremntal change:
    change the Rest API of EX00002_EC2
    instead retuning data from memoey,
    reurn data from RDS (like sql server)
        "server": RDS, user: admin 
        db: spotifydb
        table: songs

 run it locally, 
  interacting with rds on aws

  