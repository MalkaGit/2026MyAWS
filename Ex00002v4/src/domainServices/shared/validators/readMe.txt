Validators 

Goal
    getAllValoidator - validates 
        allowed fields
        allowed includes
        allowed sort keys
        paging rules
Clean (industry best practice)
    1. cloud agnostic  
    2. db agnostic 
        MySql, Posgress, Mongo, Dyname, ElasticSearch
    3. framework agnostic (express, Next.js, fastify) 
    4. domain agnostic  (song, artist)
       