
 * Folder = namespace grouping 
 * Folder ≠ module 
The file is "module" since it has export


- c# private 
  anything we define in file without export
  is not visible outside the file 

- c# public 
  The file export the class
  That makes it "file module"
  (single file = single module)


- Due to export in the file,
  the file can be imported by the file path
  if we change the file name, 
  the improt  must be updated as well :-( 
    - in all consuemrs -

- The folder that holds the file doe not have index.ts
  thus, the file can not be imported by the folder name




